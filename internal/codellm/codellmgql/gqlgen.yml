# gqlgen config for codellm's own GraphQL surface (markdown structure only).
# Separate from the monolith's repo-root gqlgen.yml and from fleet's fleetgql —
# a standalone parse/assemble API compiled into cmd/codellm. Mirrors the
# fleetgql setup (#231).
#
# Regenerate:
#   go tool github.com/99designs/gqlgen generate --config internal/codellm/codellmgql/gqlgen.yml

schema:
  - internal/codellm/codellmgql/schema.graphqls

exec:
  package: codellmgen
  layout: single-file
  filename: internal/codellm/codellmgql/codellmgen/generated.go

model:
  filename: internal/codellm/codellmgql/model/models_gen.go
  package: model

resolver:
  package: codellmgql
  layout: follow-schema
  dir: internal/codellm/codellmgql
  filename_template: "{name}.resolvers.go"

omit_slice_element_pointers: true
omit_complexity: true
skip_mod_tidy: true

# Keep the public FleetInput graph shape backed by the same Go wire types that
# Fleet uses to build $FLEET_INPUT. GraphQL intentionally omits AttachedNote
# metadata: it is an internal allowlist detail, not execution input.
models:
  JSON:
    model: github.com/99designs/gqlgen/graphql.Map
  FleetInput:
    model: trip2g/internal/fleetinput.Input
  FleetChangeInput:
    model: trip2g/internal/fleetinput.ChangeInfo
  FleetAttachedNoteInput:
    model: trip2g/internal/fleetinput.AttachedNote
