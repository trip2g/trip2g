mutation ($input: ToggleFavoriteNoteInput!) {
	payload: toggleFavoriteNote(input: $input) {
		__typename
		... on ToggleFavoriteNotePayload {
			favoriteNotes {
				pathId
			}
		}
		... on ErrorPayload {
			message
		}
	}
}
