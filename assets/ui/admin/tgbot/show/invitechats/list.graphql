query ($filter: AdminTgBotChatsFilterInput!) {
	admin {
		tgBotChats(filter: $filter) {
			nodes {
				id
				chatType
				chatTitle
				addedAt
				removedAt
				memberCount
				subgraphInvites {
					id
					subgraphId
				}
			}
		}
	}
}
