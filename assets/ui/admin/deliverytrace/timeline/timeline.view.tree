$trip2g_admin_deliverytrace_timeline $mol_view
	hops /
	sub <= bars /
	Bar!id $mol_link
		arg <= bar_arg!id *
		hint <= bar_hint!id \
		style <= bar_style!id *
		sub /
			<= Bar_run!id $mol_view
				style <= bar_run_style!id *
			<= Bar_label!id $mol_view
				sub / <= bar_label!id \
