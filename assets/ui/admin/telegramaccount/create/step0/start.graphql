mutation ($input: AdminStartTelegramAccountAuthInput!) {
	admin {
		payload: startTelegramAccountAuth(input: $input) {
			__typename
			... on AdminStartTelegramAccountAuthPayload {
				authState {
					phone
					state
					passwordHint
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
