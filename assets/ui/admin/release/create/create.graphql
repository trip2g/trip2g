mutation ($input: CreateReleaseInput!) {
	admin {
		payload: createRelease(input: $input) {
			__typename
			... on CreateReleasePayload {
				release {
					id
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
