mutation ($input: MakeReleaseLiveInput!) {
	admin {
		payload: makeReleaseLive(input: $input) {
			__typename
			... on ErrorPayload {
				message
			}
			... on MakeReleaseLivePayload {
				release {
					id
				}
			}
		}
	}
}
