query ($filter: AdminTelegramPublishNotesFilter!) {
	admin {
		allTelegramPublishNotes(filter: $filter) {
			nodes {
				id
				publishAt
				secondsUntilPublish
				publishedAt
				status
				errorCount
				noteView {
					title
				}
			}
		}
	}
}
