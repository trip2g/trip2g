mutation ($input: SendTelegramPublishNoteNowInput!) {
	admin {
		payload: sendTelegramPublishNoteNow(input: $input) {
			__typename
			... on SendTelegramPublishNoteNowPayload {
				publishNote {
					id
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
