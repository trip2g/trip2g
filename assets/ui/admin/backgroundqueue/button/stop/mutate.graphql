mutation ($input: StopBackgroundQueueInput!) {
	admin {
		payload: stopBackgroundQueue(input: $input) {
			__typename
			... on StopBackgroundQueuePayload {
				queues {
					id
					stopped
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
