mutation ($input: SetActiveGoogleOAuthCredentialsInput!) {
	admin {
		data: setActiveGoogleOAuthCredentials(input: $input) {
			__typename
			... on ErrorPayload {
				message
			}
			... on SetActiveGoogleOAuthCredentialsPayload {
				credentials {
					id
				}
			}
		}
	}
}
