mutation ($id: Int64!) {
	admin {
		data: revokeFederationSecret(id: $id) {
			__typename
			... on ErrorPayload {
				message
			}
			... on RevokeFederationSecretPayload {
				revokedId
			}
		}
	}
}
