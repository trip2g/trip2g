mutation ($kid: String!) {
	admin {
		data: rotateFederationSecret(kid: $kid) {
			__typename
			... on ErrorPayload {
				message
			}
			... on RotateFederationSecretPayload {
				kid
			}
		}
	}
}
