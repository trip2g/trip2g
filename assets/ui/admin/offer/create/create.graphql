mutation ($input: CreateOfferInput!) {
	admin {
		payload: createOffer(input: $input) {
			__typename
			... on CreateOfferPayload {
				offer {
					id
					publicId
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
