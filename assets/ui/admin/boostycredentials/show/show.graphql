query ($id: Int64!) {
	admin {
		boostyCredentials(id: $id) {
			createdAt
			deviceId
			blogName
			state

			createdBy {
				email
			}

			tiers {
				nodes {
					id
					name

					subgraphs {
						id
					}
				}
			}

			members {
				nodes {
					email
					status
				}
			}
		}
	}
}
