mutation ($input: CreateUserInput!) {
	admin {
		createUser(input: $input) {
			__typename
			... on CreateUserPayload {
				user {
					id
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
