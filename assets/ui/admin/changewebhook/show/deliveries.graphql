query ($id: Int64!) {
	admin {
		changeWebhookDeliveries(filter: { webhookId: $id, limit: 20 }) {
			nodes {
				id
				status
				responseStatus
				attempt
				durationMs
				createdAt
				completedAt
			}
		}
	}
}
