mutation ($input: CreateRedirectInput!) {
	admin {
		payload: createRedirect(input: $input) {
			__typename
			... on CreateRedirectPayload {
				redirect {
					id
				}
			}
			... on ErrorPayload {
				message
			}
		}
	}
}
