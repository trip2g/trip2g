namespace $.$$ {
	$mol_style_define( $trip2g_user_space, {
		alignItems: 'center',

		Dialog: {
			position: 'relative',
		},

		CloseButton: {
			position: 'absolute',
			top: '25px',
			right: '25px',
			cursor: 'pointer',
		}
	})

	$mol_style_define( $trip2g_user_space_name, {
		margin: '1rem',
	})
}
