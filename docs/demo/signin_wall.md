---
title: Sign-in Wall Test Page
subgraphs: signin_required
---

This page requires sign-in to view.

The `signin_required` subgraph has `require_signin` enabled, so guests see the sign-in wall instead of content.

After signing in, the full content is visible without a subscription.
